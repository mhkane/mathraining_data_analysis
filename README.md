# Mathraining country data analysis

Data analysis and reporting tools for [Mathraining](https://www.mathraining.be/), the interactive training site for the **International Mathematical Olympiad** (IMO) in French. This repo scrapes country-level user lists and resolution history from Mathraining, then builds yearly analytics and gender-breakdown reports with visualizations.

## About Mathraining

[Mathraining](https://www.mathraining.be/) is a French-language platform for training in olympiad-style mathematics. Launched in December 2014, it offers:

- **Theory** : foundations, combinatorics, geometry, number theory, algebra, functional equations, inequalities  
- **Problems** L by topic, virtual tests, and contests  
- **Statistics** L scores, recent resolutions, difficulty by chapter  

As of 2024 the site reports **~24,000 ranked users**, **~132,000 problems solved**, and **~1.3 million exercises solved**. Users come mainly from Belgium and France, plus many francophone countries (e.g. Morocco, Tunisia, Côte d'Ivoire, Algeria, Luxembourg). The goal is to prepare secondary-school students for international olympiads while welcoming anyone interested in olympiad mathematics.

## What this repo does

- **Scrapes** country user lists and profile pages from Mathraining (scores, exercises/problems solved, sign-up date, inferred gender from French grammar).
- **Scrapes** resolution history (exercises and problems with dates and points).
- **Builds** yearly and cumulative metrics (accounts, points, exercises, problems) overall and by gender.
- **Identifies** most active users by year and “top 3 / top 2 by gender” for display.
- **Produces** matplotlib figures: cumulative and per-year series, bar charts, and gender breakdowns.
- **Saves/loads** report data via pickle so you can re-run analysis and plots without re-scraping.

## Project structure

| File | Description |
|------|-------------|
| `mathraining_scraping_utils.py` | Scraping, metrics, plotting, and report build/save/load. |
| `Mathraining_Country_Report.ipynb` | Jupyter notebook: run full pipeline (Part 1), save data to pickle, then load and run custom analysis/plots (Part 2). |
| `report_data.pkl` | Saved report data (DataFrames + metadata); generated by the notebook, not committed (see `.gitignore`). |

## Requirements

- Python 3.x  
- `requests`, `beautifulsoup4`, `pandas`, `matplotlib`  
- Optional: `tqdm` for progress bars  

Install with:

```bash
pip install requests beautifulsoup4 pandas matplotlib tqdm
```

## Usage

1. Open `Mathraining_Country_Report.ipynb`.
2. Set `country_id` (e.g. `48` for Côte d'Ivoire). Country IDs match Mathraining’s `/users?country=...` filter.
3. **Part 1:** Uncomment and run the pipeline cell to call `msu.build_country_yearly_report(country_id)`, then save with `msu.save_report_data(report, "report_data.pkl")`.
4. **Part 2:** Load with `msu.load_report_data("report_data.pkl")`, then use `msu.build_report_from_data(data)` to regenerate figures and tables. Customize plots using the returned DataFrames and helpers such as `msu.plot_cumulative_single_bar(...)` and `msu.plot_per_year_single(...)`.

Be respectful with scraping: the notebook uses `sleep_seconds` (e.g. 0.3–0.4) between requests to avoid overloading the site.

## Main functions (from `mathraining_scraping_utils`)

- **Scraping:** `get_country_user_count_and_pages`, `generate_country_user_urls`, `extract_profiles_from_urls`, `scrape_profile_details`, `scrape_profile_resolutions`, `create_profiles_dataframe`, `build_events_df`
- **Metrics:** `build_yearly_metrics`, `build_yearly_metrics_by_gender`, `most_active_by_year`, `most_active_by_year_gender`, `most_active_top_n`, `most_active_top_n_by_gender`
- **Plots:** `plot_cumulative`, `plot_per_year`, `plot_cumulative_by_gender`, `plot_per_year_by_gender`, `plot_top2_active_by_gender`, and single-metric helpers
- **Pipeline:** `build_country_yearly_report`, `build_report_from_data`, `save_report_data`, `load_report_data`

## License

Use and adapt as you like. Mathraining’s content and data are subject to [Mathraining](https://www.mathraining.be/)’s own terms and policies.
